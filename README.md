# agentic-todo-mcp
A backend system that exposes Todo management tools to AI agents via the Model Context Protocol (MCP), while also providing a traditional HTTP REST API.  This allows both humans and AI agents (Claude Desktop, Cursor, etc.) to interact with the same backend.
